iam just don't have access to go on render and redeploy so i will always each time type anyting new here so i can push a change in github and the server will notice the new push so it will redeploy itself


want to redeploy again - updated AI Pipeline URL to Railway domain
redeploy: pin Python 3.12.3 via .python-version file