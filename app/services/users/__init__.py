"""User services package.

Keep package initialization lightweight to avoid circular imports.
Import concrete submodules directly where needed, e.g.:
`from app.services.users import user_service`.
"""
